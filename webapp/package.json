{
  "name": "webapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "export SET NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build": "export SET NODE_OPTIONS=--openssl-legacy-provider && next build",
    "start": "export SET NODE_OPTIONS=--openssl-legacy-provider && next start",
    "lint": "export SET NODE_OPTIONS=--openssl-legacy-provider && next lint"
  },
  "dependencies": {
    "@apollo/client": "^3.4.13",
    "babel-plugin-styled-components": "^2.0.7",
    "graphql": "^15.6.0",
    "maplibre-gl": "^2.4.0",
    "next": "11.1.2",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "styled-components": "^5.3.9",
    "swr": "^2.1.0"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.2"
  },
  "resolutions": {
    "styled-components": "^5"
  }
}
